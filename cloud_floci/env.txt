# 1. Internal Backend Proxy Routing (Keep Local for VM Internals)
# set -a && source .env && set +a
FLOCI_ENDPOINT=http://localhost:4566
FLOCI_AZURE_ENDPOINT=http://localhost:4577
FLOCI_GCP_ENDPOINT=http://localhost:4588
FLOCI_AZURE_ACCOUNT_NAME=devstoreaccount1

# 2. Global Overrides for Backend Fallbacks
AWS_ENDPOINT_URL=http://localhost:4566
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
PORT=3001
VITE_MOCK_MODE=false

# 3. Strict Frontend Vite Layer Overrides (Exposed Directly to Windows Browser)
VITE_AWS_ENDPOINT_URL=http://192.168.56.102:4566
VITE_AWS_ACCESS_KEY_ID=test
VITE_AWS_SECRET_ACCESS_KEY=test
VITE_AWS_REGION=us-east-1
AWS_CBOR_DISABLE=1
DISABLE_AMAZON_JSON=1